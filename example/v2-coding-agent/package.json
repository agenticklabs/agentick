{
  "name": "example-v2-coding-agent",
  "version": "0.0.1",
  "private": true,
  "description": "Agentick v2 end-to-end example — a naive coding agent, driven over the wire by a client. Showcases the full client-side ergonomics built for v2: the batteries-included @agentick/client-next bundle, session sub-handles (session.knobs / session.tasks / session.elicitations), the execution token stream, and onLog — against a real JSX coding agent whose tools gate destructive writes through elicitation and run shell commands as tasks.",
  "type": "module",
  "scripts": {
    "dev": "tsx --tsconfig tsconfig.json src/index.ts",
    "start": "tsx --tsconfig tsconfig.json src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agentick/client-next": "workspace:*",
    "@agentick/gateway-next": "workspace:*",
    "@agentick/knobs-next": "workspace:*",
    "@agentick/model-ai-sdk-next": "workspace:*",
    "@agentick/reconciler-react-next": "workspace:*",
    "@agentick/spec-next": "workspace:*",
    "@agentick/timeline-next": "workspace:*",
    "@agentick/transport-in-process-next": "workspace:*",
    "@agentick/transport-next": "workspace:*",
    "@ai-sdk/openai": "^2.0.0",
    "dotenv": "^16.4.5",
    "react": "^19.2.4",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.5.4",
    "@types/react": "^19.2.13",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
