# Roadmap

Agentick has a single roadmap, and it lives with the v2 work in progress — not here.

- **What's planned and in flight:** [`docs/proposals/v2/IMPLEMENTATION-PLAN.md`](docs/proposals/v2/IMPLEMENTATION-PLAN.md) — the phased rollout plan.
- **What's decided and done:** [`docs/proposals/v2/STATUS.md`](docs/proposals/v2/STATUS.md) — the running progress log.
- **The architecture behind it:** [`docs/proposals/v2/blueprint/`](docs/proposals/v2/blueprint/) — the ADRs (start with `00-overview.md`).

v1 is the stable published line under `packages/`; it is in maintenance, not active feature development. v2 is built under `packages/` on the `feat/v2` branch.

This file is a pointer so there is no competing second source of truth for the roadmap.
