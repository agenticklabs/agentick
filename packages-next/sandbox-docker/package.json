{
  "name": "@agentick/sandbox-docker-next",
  "version": "0.0.0",
  "private": true,
  "description": "Docker SandboxProvider — one container per sandbox, exec via docker exec (streamed), fs via docker exec, atomic editFile via applyEdits, coarse egress via NetworkMode. Capability-tier: no runtime mounts (ADR 59).",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "oxlint src/",
    "format:check": "oxfmt --check src/",
    "clean": "rm -rf dist tsconfig.build.tsbuildinfo"
  },
  "dependencies": {
    "@agentick/sandbox-next": "workspace:*"
  }
}
