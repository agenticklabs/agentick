/**
 * Canonical extension name for this package. Used as the
 * `binding.extensionName` value for the model-facing `resource_*` tools
 * that {@link withResources} auto-registers — adopters can filter /
 * introspect the per-session tool list by extension origin.
 */
export const EXTENSION_NAME = "@agentick/resources-next";
