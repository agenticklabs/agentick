/**
 * Server-side protocol contracts — the symmetric counterpart to
 * `./client/`. Where `client/` defines what a client exposes,
 * `server/` defines what the gateway owns on the wire.
 *
 * @see docs/proposals/v2/blueprint/84-gateway-lifecycle-and-transports.md
 */

export * from "./transport.js";
