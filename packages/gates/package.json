{
  "name": "@agentick/gates",
  "version": "0.0.0",
  "private": true,
  "description": "Gates — knob-backed continuation conditions. Pure hook layer over @agentick/knobs + reconciler-react. Not a harness; gates have no independent state — the gate's value IS a knob value. Bundled into the agentick metapackage; not published independently.",
  "keywords": [
    "agent",
    "agentick",
    "ai",
    "gates",
    "knobs"
  ],
  "license": "MIT",
  "author": "Ryan Lindgren",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/agenticklabs/agentick.git",
    "directory": "packages/gates"
  },
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.build.json --noEmit"
  },
  "dependencies": {
    "@agentick/knobs": "workspace:*",
    "@agentick/reconciler": "workspace:*",
    "@agentick/reconciler-react": "workspace:*",
    "@agentick/spec": "workspace:*"
  },
  "devDependencies": {
    "@agentick/knobs": "workspace:*",
    "@agentick/runtime": "workspace:*",
    "@types/react": "^19.2.5"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  }
}
