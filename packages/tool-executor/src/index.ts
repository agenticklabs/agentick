/**
 * @agentick/tool-executor — reference tool executor harness.
 *
 * Reference implementation of `ToolExecutorProtocol` from
 * `@agentick/spec`. Phase 4a scaffold — implementation lands
 * incrementally in 4a.4–4a.7.
 *
 * @see docs/proposals/v2/blueprint/07-tool-executor.md
 */

// Phase 4a.3 is the package scaffold; substantive exports land in
// 4a.4 (harness skeleton + dispatch happy path) onward. The empty
// export keeps the module valid until then.
export {};
