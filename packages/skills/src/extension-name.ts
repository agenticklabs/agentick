/**
 * Canonical extension name for this package. Used as the
 * `binding.extensionName` value for the model-facing `skill_*` tools
 * that {@link withSkills} auto-registers — adopters can filter /
 * introspect the per-session tool list by extension origin.
 */
export const EXTENSION_NAME = "@agentick/skills";
