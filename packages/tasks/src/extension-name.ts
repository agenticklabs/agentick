/**
 * Canonical extension name for this package. Used as the
 * `binding.extensionName` value for tools that {@link withTasks}
 * auto-registers — adopters can filter / introspect the per-session
 * tool list by extension origin.
 */
export const EXTENSION_NAME = "@agentick/tasks";
