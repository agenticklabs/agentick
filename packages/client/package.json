{
  "name": "@tentickle/client",
  "version": "0.0.1",
  "description": "Client SDK for Tentickle - channel connection, framework sugar, transport implementations",
  "keywords": [
    "agent",
    "ai",
    "client",
    "channels",
    "sse",
    "websocket"
  ],
  "license": "ISC",
  "author": "Ryan Lindgren",
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./transports": "./src/transports/index.ts"
  },
  "publishConfig": {
    "access": "public",
    "exports": {
      ".": {
        "types": "./dist/index.d.ts",
        "import": "./dist/index.js"
      },
      "./transports": {
        "types": "./dist/transports/index.d.ts",
        "import": "./dist/transports/index.js"
      }
    },
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "echo \"Tests run from workspace root\"",
    "typecheck": "tsc -p tsconfig.build.json --noEmit",
    "lint": "oxlint src/",
    "format:check": "oxfmt --check src/",
    "clean": "rm -rf dist",
    "prepublishOnly": "pnpm build",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@tentickle/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
