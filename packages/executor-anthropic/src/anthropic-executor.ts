/**
 * `AnthropicExecutor` — placeholder.
 *
 * The executor body is intentionally not implemented in this scaffold.
 * See `docs/proposals/v2/anthropic-adapter-plan.md` for the full
 * implementation plan; the body lands in a follow-up commit once the
 * plan is reviewed.
 */

export interface AnthropicExecutorOptions {
  /** Placeholder — see implementation plan for the full shape. */
  readonly model?: string;
}

export {};
